import { Icon, type LucideProps } from "lucide-react";
import { planet } from "@lucide/lab";

/** Lucide Lab「planet」— 带环星球，语义贴近知识宇宙 */
export default function Planet(props: LucideProps) {
  return <Icon iconNode={planet} {...props} />;
}
